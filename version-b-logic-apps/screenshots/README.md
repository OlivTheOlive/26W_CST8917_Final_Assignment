# Version B — screenshots for submission

Capture and save PNGs here (examples):

1. **Service Bus** — Namespace overview showing the **queue** (`expense-requests`) and **topic** (`expense-outcomes`) with **three subscriptions** (approved, rejected, escalated) and their **SQL filters** on the message **application property** `outcome`.
2. **Logic App run history** — A successful run showing the **validation** branch, **auto-approve** path for amount \< 100, and the **manager / polling** path for amount ≥ 100.
3. **Logic App designer** — Zoomed view of **conditions** and **Service Bus** send actions with **topic** properties.
4. **Email** — Inbox screenshot of the **employee** notification for each outcome (approved, rejected, escalated).
5. **Topic subscriptions** — Peek or metrics showing messages routed to the expected subscription after a test run.

Replace placeholder filenames in your report if your course requires specific names.
